# Maintainer: Alex Virelles <thesnakewitcher@gmail.com>
pkgname="tsw-tools"
pkgver="1.0.0"
pkgrel=1
pkgdesc="tools packages"
arch=('x86_64')
license=('MIT')
depends=(
    jq jc yq   # cli json management , check jo, jshon
    xsv        # csv management in cli
    direnv     # env variables management
    fpm        # software packaging management
    zoxide     # fs navigation
    fzf        # fuzzy finder
    fd         # fs searching
    ripgrep    # content searching
    sad        # batch file editing
    atuin      # cli history
    procs      # process management
    dust       # storage visualization
    gum        # shell script enhacer
    glow       # markdown previewer

    # engineering
    # kicad freecad qgis

    # encryption
    age         # file encryption tool
    veracrypt   # disk encryption tool

    scrcp            # image sharing
    qtqr qrcp qrtool # io with qr codes 
    pandoc-cli       # documentation convertion

    # upx        # compressor for binary executables

    # dnote-cli-bin  # cli notes

    # chezmoi    # dotfiles package manager
    nitroshare # lan file transfer https://archlinux.org/packages/?name=nitroshare
    nextcloud  # local cloud server
    # syncthing  # open source continuos replication / cluster synchronization thing
    # avahi    # service discovery for linux
    # artem-bin  # pic to ascii art converter
    # urlwatch   # websites notifications

    # "graphviz"
    # quarto-cli-bin
    # gocr # ocr
    # hoard # cli command organizer
    # ffmpeg # video converter, alternative: handbrake  
    #
    # czkawka-gui-bin # find duplicates/empty/similiar files
    #
    # metabase(business intelligence and analytics)
    # syncthing
    # portainer-bin(docker management ui)
    # uptime-kuma(self-hostedm monitoring tool)
    # vaultwarden(unofficial bitwarden compatible server)

    # bottles steam wine

    # AI
    # aichat
    tgpt     # ai models comunication from cli
    ollama   # local LLM management

    # malware
    # calmav # anti malware
)
